{
  "type": "mysql",
  "host": "localhost",
  "port": 3306,
  "username": "user",
  "password": "password",
  "database": "db",
  "entities": [
    "dist/db/**/*.entity{.ts,.js}"
  ],
  "migrations": [
    "dist/db/migrations/*{.ts,.js}"
  ],
  "synchronize": true,
  "cli": {
    "migrationsDir": "src/db/migrations"
  }
}
